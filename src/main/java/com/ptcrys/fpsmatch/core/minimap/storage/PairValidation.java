package com.ptcrys.fpsmatch.core.minimap.storage;

/** Describes how a persisted source/runtime pair was validated before publication. */
public enum PairValidation {
    FULL,
    METADATA_TRUSTED
}
